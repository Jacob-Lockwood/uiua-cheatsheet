# uiua cheatsheet

to develop: install nodejs, pnpm, and uiua, then run `pnpm i` to install dependencies and `pnpm dev` to start a dev server on `localhost:8080`.

`primitives.json` is sourced from [here](https://www.uiua.org/primitives.json).
